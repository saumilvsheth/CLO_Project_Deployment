"""Phase 2–3 placeholders: graph write and agent tools are not implemented yet."""

from __future__ import annotations


def graph_backfill_not_implemented() -> str:
    return (
        "Phase 2: write entities (deal, tranche, obligor, trustee, manager) to Cosmos DB "
        "and index community summaries in Azure AI Search."
    )


def agent_query_not_implemented() -> str:
    return (
        "Phase 3: Foundry agent with tools graph_traverse, vector_search, "
        "and get_compliance_test — the model never queries stores directly."
    )
